// utilizando apenas o compilador direto do CLI com o comando npx tsc hello-world.ts
// using just the compiler directly from CLI with the command npx tsc hello-world.ts

console.log('Hello, world! With TypeScript!');

// Primeiro passo é utilizar o comando npx tsc hello-world.ts
// first step is to use the command npx tsc hello-wold.ts

// logo após, utilizar o comando node hello-world.js porque com esse comando será criado um arquivo novo js
// then, use the command node hello-world.js because with this commando, a new js file will be created